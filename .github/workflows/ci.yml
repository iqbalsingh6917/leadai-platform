name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint
        run: |
          # TODO: Add lint commands when apps are scaffolded
          # Example: cd apps/api && npm run lint
          # Example: cd apps/web && npm run lint
          echo "Lint step placeholder — to be implemented"

      - name: Test
        run: |
          # TODO: Add test commands when apps are scaffolded
          # Example: cd apps/api && npm run test
          # Example: cd apps/web && npm run test
          # Example: cd apps/ai-services && pytest
          echo "Test step placeholder — to be implemented"

      - name: Build
        run: |
          # TODO: Add build commands when apps are scaffolded
          # Example: cd apps/api && npm run build
          # Example: cd apps/web && npm run build
          echo "Build step placeholder — to be implemented"
